/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "images.immediate.co.uk"],
  },
  // Add other configurations here as needed
};

export default nextConfig;
